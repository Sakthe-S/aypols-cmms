{
  "name": "aypols-cmms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:seed": "npx tsx prisma/seed.ts",
    "sync": "git pull --ff-only origin main",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "node -e \"const fs=require('fs');const{Pool}=require('pg');const p=new Pool({connectionString:process.env.DATABASE_URL});(async()=>{const glob=require('path');const dir=require('fs').readdirSync('migrations').filter(f=>f.endsWith('.sql')).sort();for(const f of dir){console.log('Applying',f);await p.query(fs.readFileSync('migrations/'+f,'utf8'));}console.log('Done. '+dir.length+' migration(s).');})().catch(e=>{console.error('Migration failed:',e.message);process.exit(1);}).finally(()=>p.end())\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "clsx": "^2.1.1",
    "lucide-react": "^0.441.0",
    "next": "14.2.12",
    "next-auth": "^4.24.8",
    "pg": "^8.13.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.5.5",
    "@types/pg": "^8.11.10",
    "@types/react": "^18.3.8",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.20",
    "eslint": "^8.57.1",
    "eslint-config-next": "14.2.12",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.12",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^4.1.11"
  }
}
